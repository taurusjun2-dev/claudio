{
  "name": "agent-loop",
  "version": "0.2.0",
  "description": "Lightweight LLM Agent Loop — tool calling skeleton for any Node.js project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/", "src/"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "example": "tsx examples/basic.ts"
  },
  "dependencies": {
    "ai": "^6.0.0",
    "@ai-sdk/openai": "^2.0.0",
    "zod": "^4.0.0"
  },
  "peerDependencies": {
    "ai": "^6.0.0",
    "zod": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "ai": { "optional": false },
    "zod": { "optional": false }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
